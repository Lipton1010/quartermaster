# Quartermaster 1.0.0 Release Gate

Quartermaster 1.0.0 is a release candidate. This gate is **closed** until every required check below has reviewable evidence. Nothing in this document authorizes merging, tagging, publication, or installation in a production world.

## Current decision

**Not approved for release.** The gate remains **closed** as of 2026-08-24. **Merge to `main` is not authorized.**

**Committed candidate:** security and P2 remediations landed at `47347b1` on `feat/system-agnostic`; this gate doc records the completed live matrix on that commit plus gate-doc reconciliation commits.

**Dual-audit (Grok + Kimi, 2026-08-24):** no P0 findings; no demonstrated P1 code hole on `47347b1`. Accepted/deferred P2s: in-memory GM rate-limit not persisted across reload; wrap/single-GM ack race documented; query-sender LIFO stack ordering recorded; CSB first-boot `quartermaster.vaultActorType=character` operator note (P2, not P0).

**Six-cell live matrix (2026-08-24):** all **Passed** on capture-phase interceptor SHA-256 `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD` — Foundry **v14.367** (D&D 5e 5.3.3, PF2e 8.4.0, CSB 6.0.2) and Foundry **v13.351** (D&D 5e 5.3.3, PF2e 7.12.2, CSB 5.2.1). Prior `B3B9DD8C…` CSB native-sheet evidence is superseded; do not promote it as current proof.

**Remaining blockers:** (1) validated `artifacts/system-agnostic/module.zip` rebuild — current archive stale at SHA-256 `ac909126…` (predates `47347b1`, lacks `query-sender.js` and related scripts); (2) final dual-audit sign-off and explicit Paul authorization before merge, tag, or publication.

## Gate summary

| Gate | Status | Evidence and next action |
| --- | --- | --- |
| Version and system-agnostic manifest | Passed on committed candidate | `module.json` reports version `1.0.0`, no system relationship, and a version-specific download URL. |
| Headless automated suite | **Passed** on `47347b1` (2026-08-24) | `npm test` reported **117/117** on committed HEAD including `query-sender`, `drag-drop`, and `mutation-rate-limit` unit tests. |
| Final automated rerun | **Passed** (2026-08-24) | Complete suite rerun at `47347b1` before gate-doc reconciliation commit. |
| Clean release package | **Stale — rebuild required** | Current `artifacts/system-agnostic/module.zip` SHA-256 `ac909126…` predates `47347b1` and lacks `query-sender.js`, capture-phase interceptor, and rate-limit scripts. Rebuild immediately after gate-doc commit. |
| Live Foundry compatibility matrix | **Passed** (six cells, interceptor `51E7246F…`) | v14.367: D&D 5e 5.3.3, PF2e 8.4.0, CSB 6.0.2 — full minimum matrix. v13.351: D&D 5e 5.3.3 (Player2/restart/rate-limit), PF2e **7.12.2**, CSB **5.2.1** (incl. token UUID, hostile, hide/reveal, reload, generic CUR) — full minimum matrix (2026-08-24). CSB native-sheet egress gap closed; do not cite stale `B3B9DD8C…` as current CSB proof. |
| Live v0.1.8-to-v1 migration rehearsal | **Passed** | Synthetic v0.1.8 fixture world upgraded against the fixed candidate; see [MIGRATION-REPORT.md](MIGRATION-REPORT.md). |
| Documentation review | In progress | Gate doc reconciled 2026-08-24; final release date only after approval. |
| Merge, tag, manual installation, and publication | Not authorized | Each remains a separate user-authorized operation after this gate is approved. |

## Required live Foundry matrix

`Passed` means every required check in "Minimum live checks for every matrix cell" was exercised and produced the expected result. `Failed` means the cell was fully exercised and at least one required check did not pass; the specific defect is documented in the evidence section below. `Pending` means no matrix result has been supplied. `Unprovisioned` describes the current test environment, not product compatibility.

| Foundry VTT | Game system | Required system version | Test status | Environment | Result notes |
| --- | --- | --- | --- | --- | --- |
| 14.367 | D&D 5e | 5.3.3 | **Passed** on interceptor `51E7246F…` (2026-08-24) | Isolated `test-data/v14-dnd5e`, port **30001**, world `quartermaster-v14-dnd-isolated` | Full minimum matrix on interceptor including linked/token transfers, native GP, hostile reject, hide/reveal, reload, **process-restart persistence**, **mutation rate-limit spot-check**, **Player2**, query-sender sanity. Prior v14.365 evidence against committed candidates (`c640109`, `7d9b55f`) stands for historical audit trail. |
| 14.367 | Pathfinder 2e | 8.4.0 (see deviation note) | **Passed** on interceptor `51E7246F…` (2026-08-24) | Isolated `test-data/v14-dnd5e`, world `quartermaster-v14-pf2e-isolated` | Compendium `Dagger` popup ingress; marked native-sheet egress on `CharacterSheetPF2e` (`preventDefault`/`stopImmediatePropagation`); unlinked Token Actor UUID ingress/egress; unmarked drops inert. Prior v14.365 native-coin and popup evidence against `7d9b55f` stands. |
| 14.367 | Custom System Builder | 6.0.2 | **Passed** on interceptor `51E7246F…` (2026-08-24) | Isolated `test-data/v14-dnd5e`, world `quartermaster-v14-csb-isolated` | Native-sheet capture egress re-verified (`preventDefault`/`stopImmediatePropagation`, single copy, vault emptied). Prior popup/token/Player2/reload/restart evidence stands. v14.365 CSB failure history (native-sheet bypass) documented below; closed by Milestone 2 interceptor fix. |
| 13.351 | D&D 5e | 5.3.3 | **Passed** on interceptor `51E7246F…` plus live query-identity/rate-limit follow-up (2026-08-24) | Isolated `test-data/v13-351-dnd5e`, port **30011**, world `quartermaster-v13-dnd-isolated` | Prior linked/token/loot-prep/GP/hostile/reload checks stand. **Player2 fail-closed and owned ingress passed** after the v13 `userQuery` identity bridge (`destination-actor-not-owned`, staged `invalid-transfer-boundary`, owned vault ingress success). **Process-restart persistence passed.** **Mutation rate-limit live spot-check passed** (`max=1` → second `CURRENCY_CHANGE` returned `rate-limited`, GP +1 only). |
| 13.351 | Pathfinder 2e | 7.12.2 | **Passed** on interceptor `51E7246F…` (2026-08-24) | Isolated `test-data/v13-351-pf2e`, port **30012**, world `quartermaster-v13-pf2e-isolated` | Manifest verified **7.12.2**; module interceptor SHA-256 `51E7246F…`. **Passed:** GM join, module init, popup ingress (compendium Dagger), native-sheet marked egress on `CharacterSheetPF2e`, unmarked drop inert, **unlinked Token Actor UUID transfer** (equipment Dagger), **process-restart persistence**, **mutation rate-limit spot-check** (`max=1`, gp), **Player2** (query-sender identity), **native coin Item transfer** (compendium `Gold Pieces` with `sizeSensitive: undefined`, popup ingress + `transferInventoryItemToActor` egress), **GM hostile** (`invalid-transfer-boundary`, no mutation), **hide/reveal** (inventory GM hide + Loot Prep reveal button), **client reload** (QM active, vault/staging/CUR intact, popup opens). Verified `--dataPath`, `--noupnp`, `--noupupdate`. |
| 13.351 | Custom System Builder | 5.2.1 | **Passed** on interceptor `51E7246F…` (2026-08-24) | Isolated `test-data/v13-351-csb`, port **30013**, world `quartermaster-v13-csb-isolated` | Manifest verified **5.2.1**; module interceptor SHA-256 `51E7246F…`. First boot required `quartermaster.vaultActorType=character` (P2 operator note). **Passed:** popup ingress, native-sheet marked egress, unmarked drop inert, **process-restart persistence**, **mutation rate-limit spot-check** (`qm-cur-default`, max=1), **Player2**, **GM hostile** (`invalid-transfer-boundary`), **hide/reveal** (GM hide + Loot Prep reveal), **client reload**, **unlinked token UUID transfer** (`Scene.NUEDEFAULTSCENE0.Token.Wcd1s1Qyv3Z50qGW.Actor.9rd11PNJEpwapR1C`, popup ingress + capture egress `preventDefault:1`), **generic currency UI** (+5 CUR, 1→6). Verified `--dataPath`, `--noupnp`, `--noupupdate`, `--headless`. |

## v14.365 / D&D 5e 5.3.3 evidence — Passed

Tester: automated verification session. Date: 2026-08-10. Platform: Windows 11, Foundry v14 Build 365, headless `node main.js`, isolated data path `test-data/v14-dnd5e` (world `quartermaster-v14-dnd-isolated`). This session did not open a production world or modify the installed module. The v1 candidate module (`artifacts/system-agnostic/module.zip`, SHA-256 `5e7f0b7eca6dfe9e63a272c669d02c964615107a6f12f6d3c40e8ffec384c76e`) was freshly re-extracted into the isolated `Data/modules/quartermaster` folder before testing (the folder previously held a mismatched 102-file tree from an earlier session; a clean 59-file extraction from the validated zip replaced it).

An earlier 2026-08-09 partial session had already established, and this session re-confirmed by direct inspection before the restart:

- fresh schema-1 initialization and creation of separate shared and private storage Actors;
- storage-Actor suppression from the sidebar (only the player-owned `Test Hero` Actor listed; both `Quartermaster Vault` and `Quartermaster Staging` absent) and the expected `Shared Party Inventory`, `GM Loot Prep`, and `Transaction Log` buttons;
- the D&D 5e `0 lb / 500 lb` load display and a native GP mutation;
- a GM linked-Actor Item round trip and a player-owned linked-Actor egress;
- GM hide to private staging, Player2 invisibility, and GM reveal back to shared storage;
- Player2 storage privacy and absence of GM-only controls;
- rejection of a hostile same-Actor/direction request as `invalid-transfer-boundary`, with no Item mutation;
- synthetic Token Actor ingress and egress, with the Item returned to its source; and
- a healthy client reload before the server restart.

**This session's addition — the previously-missing check.** The Foundry process was stopped (`Stop-Process`) and relaunched clean against the same data path and port. A client reconnected via the browser tool, launched the world, and logged in as Gamemaster:

- the server logged `Launching World | Complete` and a clean `Server started and listening on port 30001`, with no lock-file conflicts on this cycle;
- the Quartermaster console sequence was clean: `Ready`, `Backing actor present: Quartermaster Vault (...)`, `Staging actor present: Quartermaster Staging (...)`, `Operation coordinator initialized`, `Socket handler registered (authenticated query: yes)`, `v1.0.0 loaded`, with no errors besides the benign, expected `WebSocket ... failed` / `lost connection` entries left over from the *old*, now-dead socket as the browser tab reconnected — the *new* socket connected cleanly;
- `game.ready` was `true`, all three Actors (`Quartermaster Staging`, `Quartermaster Vault`, `Test Hero`) were present and intact, the sidebar directory still listed only `Test Hero`, and all three Quartermaster toolbar buttons were present and functional;
- opening `Shared Party Inventory` rendered correctly post-restart (`0 lb / 500 lb`, `1 GP`, vault contents unchanged from before the restart).

No console errors were observed after reconnection other than the expected stale-socket entries described above. This closes the previously-open item; the D&D 5e 5.3.3 cell is **Passed** with no known defects.

**Re-verified live against the true final candidate — 2026-08-10.**

An independent audit of this release-gate document found that everything above this line, and the gate-summary/decision language claiming all three v14.365 cells had live evidence against the fixed candidate, overstated D&D 5e's actual coverage. The checklist above was carried forward from a 2026-08-09 session that tested only against the pre-fix candidate `6237480` — before either `523f788` (the migration Set-field fix) or `4ce4706` (the `undefined`-key write-verification fix) had landed. The note that D&D 5e was "unaffected by this fix" was a reasonable-sounding inference — `4ce4706` touches `scripts/transaction-log.js`, which is on the same Item-transfer path D&D 5e uses — but it was never actually exercised live against a build containing the fix. This session closed that gap with a genuine live re-test.

**Candidate tested.** Commit `c640109499e5c8b173c85ff47194d5c294f31304` (the `feat/system-agnostic` HEAD at the start of this session). This commit contains no code changes relative to `7d9b55f9613e76444a600de4d162ccd1bd9893b5`/`4ce4706` — `git diff 7d9b55f c640109` touches only `CHANGELOG.md`, `docs/MIGRATION-REPORT.md`, and `docs/RELEASE-GATE.md` — so this is a genuine live test of the same fixed code, from the current tip of the branch. The archive was rebuilt with `git archive --format=zip --prefix=quartermaster/ HEAD` (a pre-existing, unrelated uncommitted `README.md` change was left untouched and uncommitted; `git archive` reads only the committed tree, so it had no effect on the package) and validated with `tools/validate-package.mjs`: 67 clean entries, SHA-256 `d4a46622b7750a8bfad031fc3a01cf9e12c162fde98379f07cdfa6bbae78b299` — matching the final package recorded in the gate summary above. This checksum was verified against the built archive both before and after extraction into the isolated `test-data/v14-dnd5e/Data/modules/quartermaster` folder (which fully replaced the folder's prior contents), and the presence of the fix in the running module was confirmed directly by inspecting the extracted `scripts/transaction-log.js` for the `value[key] !== undefined` filter in `stableStringify`.

**Setup.** Foundry was launched headless against the isolated `test-data/v14-dnd5e` data path on port 30001. The world `quartermaster-v14-dnd-isolated` was opened as Gamemaster. Module init was clean: `Quartermaster | Ready`, `Backing actor present: Quartermaster Vault`, `Staging actor present: Quartermaster Staging`, `Operation coordinator initialized`, `Socket handler registered`, `v1.0.0 loaded`, no module errors (the only console error present was the environment's benign screen-resolution warning, unrelated to Quartermaster).

**Genuine repro condition confirmed present.** Rather than reuse the pre-existing hand-crafted `Test Gem` fixture on `Test Hero`, a real, completely unmodified compendium weapon (`Dagger`, `dnd5e.items` pack) was pulled and inspected via `toObject()`. Its raw source carries eleven `undefined`-valued leaf keys inherent to D&D 5e's own item schema (`system.unidentified.name`, `system.magicalBonus`, `system.activities.*.uses.max`, `system.activities.*.name`, `system.activities.*.visibility.identifier`, `system.activities.*.visibility.level.min`/`max`, `system.ammunition.type`, `system.crew.max`, `system.speed`, `system.mastery`). D&D 5e's native item schema does carry the class of field the original defect was about, so this was a real, live exercise of the exact failure mode — not a case where there was nothing to test.

- **Linked owned Actor (`Test Hero`).** The compendium `Dagger` was created directly on `Test Hero`, then transferred into the vault via a real `DragEvent`/`DataTransfer` dispatch (`{type: "Item", uuid: "Actor.<Test Hero id>.Item.<id>"}`) at the inventory popup's registered `.quartermaster-inventory-root` drop zone, invoking the actual `onPopupDrop` handler. Result: `Dagger added to Quartermaster Vault.`, no error; the transaction log recorded a full `transfer.ingress.claim` → `transfer.ingress.commit` pair with `matchesPredictedId: true`. The vault's `Dagger` was then returned to `Test Hero` via the module's own `transferInventoryItemToActor` (the same function the popup's item-row "Add to Actor" action calls) — egress also succeeded cleanly, fully restoring the Item to the Actor and removing it from the vault.
- **Unlinked Token Actor.** A fresh `Token` document (`actorLink: false`) was created on the world's default scene, giving it its own synthetic Actor (`Synthetic Token Source`). A second, independent, untouched compendium `Dagger` was created on that synthetic Actor and dragged into the vault the same way, using its `Scene.<id>.Token.<id>.Actor.<id>.Item.<id>` UUID. Result: `Dagger added to Quartermaster Vault.`, no error. It was then transferred back via `transferInventoryItemToActor`; egress succeeded cleanly, fully restoring the Item to the token's synthetic Actor.
- **No `canonical-transaction-log-verification-failed` error occurred in any of the four transfers** (two directions × two Actor types), against a genuinely reproducing Item.
- **Native currency mutation (second write-verification path).** Using the inventory popup's own GP quick-add control (shift-click on the GP tile, the module's real `currency-add` action handler), 1 GP was added to the vault. Result: `1 GP added. New balance: 2 GP.`, no error; the transaction log recorded a `currency.commit` entry with success status.
- **Hide/reveal spot-check.** The vault's `Dagger` was hidden to staging via the module's own GM context-menu action (`handleInventoryGMAction("hide-item", ...)`): confirmed moved off the vault and onto `Quartermaster Staging`. It was then revealed back via the Loot Prep app's own `reveal-item` button: confirmed moved back onto the vault and off staging. No errors.
- **Clean restart/reconnect.** The Foundry process was stopped and relaunched clean against the same isolated data path and port. A client reconnected, launched the world, and logged in as Gamemaster. Console showed a clean re-init (`Quartermaster | Init`, `Settings registered`, `Ready`, `Backing actor present`, `Staging actor present`, `Operation coordinator initialized`, `Socket handler registered`, `v1.0.0 loaded`), no errors besides the expected stale-socket entries from the old dead connection. Post-restart state was confirmed intact and correctly persisted: the vault held the `Dagger` and `2 GP`; staging was empty; `Test Hero` held only its original `Test Gem` (no duplicate, no leftover `Dagger`); the token's synthetic Actor held its own `Dagger`.

**This closes the previously-open evidence gap. The D&D 5e 5.3.3 cell is confirmed Passed against the true final candidate, live, with no regression found.**

## v14.365 / Pathfinder 2e 8.4.0 evidence — Passed (re-verified against the fix)

Tester: automated verification session. Date: 2026-08-10. Platform: Windows 11, Foundry v14 Build 365, headless `node main.js`, isolated data path `test-data/v14-dnd5e` (world `quartermaster-v14-pf2e-isolated`, created fresh for this cell).

**Deviation from the matrix.** The task specified Pathfinder 2e 8.3.0. Foundry's package browser (`Install System`) only offers the current release, which was 8.4.0 (verified for Foundry 14.365) at test time; no version picker or older-release manifest was available through the normal install UI, and no login/credential wall was encountered installing it. Testing proceeded against 8.4.0 as the closest available official build; 8.3.0 was not separately obtained.

**Setup.** Installed the "Pathfinder Second Edition" system (stwlam, v8.4.0) via the in-app package browser — no login required. Created a new world bound to `pf2e`, enabled Quartermaster via Module Management, and confirmed a clean schema-1 initialization (`Quartermaster | Ready`, `Backing actor created: Quartermaster Vault`, `Staging actor created: Quartermaster Staging`, `Storage migration to schema 1 complete`, `v1.0.0 loaded`, no errors). Created a linked PC Actor ("Test Hero PF2e", owned by a new `Player2` user) with a compendium `Dagger` Item and native PF2e coin Items (15 gp, 5 sp via `actor.inventory.addCoins`), plus an unlinked Token Actor ("Synthetic Token Source") on the default scene.

**Passed checks:**

- Sidebar suppression for both GM and Player2 — only player-owned/visible Actors listed, both storage Actors absent.
- Linked-Actor Item round trip: the `Dagger` moved cleanly vault-ward and back (ingress then egress), confirmed removed from the source and present at the destination each time, with matching `transfer.ingress.commit` / `transfer.egress.commit` transaction-log entries.
- Unlinked Token Actor Item round trip: same result using the synthetic token's item, via its `Scene.<id>.Token.<id>.Actor.<id>.Item.<id>` UUID.
- Native currency **display**: the Currency Manager and inventory panel correctly showed PF2e's real PP/GP/SP/CP denominations with correct exchange rates (`1 PP = 10 GP`, `1 SP = 0.1 GP`, `1 CP = 0.01 GP`) and PF2e's native "1,000 coins per Bulk" rule — this is the system's actual currency model, not the generic `CUR` fallback.
- Load/value fields: the `Dagger` correctly showed PF2e bulk (`L`) and price (`2 SP`) supplied by the adapter.
- Player privacy: Player2's `Shared Party Inventory` panel showed no `Manage Currencies` or `Create Item` buttons (GM-only, correctly hidden); the hidden `Dagger` did not appear ("The vault is empty") while staged; `api.ui.openLootPrep()` was a silent no-op for Player2 (GM-only app, correctly refused). *Caveat:* Foundry's client-side architecture syncs full Actor/flag data (including the GM-only staging Actor's canonical transaction log with raw Item snapshots) to every connected client regardless of ownership; a technically capable player could read this via the browser console. Quartermaster's own API and UI both correctly gate on `game.user.isGM` and this is not unique to Quartermaster — it is a general Foundry VTT platform trait — but it means the privacy boundary is enforced at the application layer, not by withholding data from the wire.
- Hostile-request rejection: a same-Actor/same-boundary request was rejected `invalid-transfer-boundary`; a request sourced from an Actor the requesting player did not own was rejected `source-actor-not-owned`; a direct attempt by Player2 to egress a *staged* (GM-only) Item straight to their own Actor, bypassing reveal, was rejected `invalid-transfer-boundary`. No Item mutation occurred in any case.
- Hide/reveal: GM hid the `Dagger` to staging (moved off the vault, appeared on staging, invisible to Player2's live-open inventory panel), then revealed it; Player2's already-open panel updated live via socket push to show the `Dagger` again, with correct bulk/price, with no manual refresh.
- Reload/restart/reconnect: Player2's client reload preserved session and vault state. A full server-process restart and reconnect of both the GM and Player2 clients produced a clean Quartermaster init sequence, an intact module/world, correct sidebar suppression, and no new console errors for either client.

**Failed check — required native-currency exercise.**

Dragging a native PF2e coin Item (`Gold Pieces` or `Silver Pieces`, both untouched, straight-from-compendium Treasure Items) from the linked Actor onto the `Shared Party Inventory` drop zone reproducibly fails. The client console shows:

```
Quartermaster | drop received {hasData: true, type: Item, uuid: Actor.<id>.Item.<id>}
Quartermaster | realItemTransfer threw Error: canonical-transaction-log-verification-failed
    at scripts/async-lock.js:49:14
Transfer failed: canonical-transaction-log-verification-failed
```

**Root cause.** `scripts/transaction-log.js`'s `writeVerifiedLog()` writes the transfer's claim entry (which embeds a full raw snapshot of the Item being moved, `itemData: sourceItem.toObject()`, per `scripts/claim-commit.js`) via `actor.setFlag(...)`, then immediately reads the flag back via `actor.getFlag(...)` and deep-compares it against the value it intended to write; a mismatch throws `${label}-verification-failed`. The "intended" value is built with `foundry.utils.deepClone`, which preserves object keys whose value is `undefined`. Real Foundry flag persistence does not: any key with an `undefined` value is silently dropped on the actual write/read round-trip. `Gold Pieces`/`Silver Pieces` (and every stock PF2e Treasure Item checked, including straight from the `pf2e.equipment-srd` compendium, untouched) carry `system.price.sizeSensitive === undefined`. That one key is present in the "intended" clone and absent from the "retained" read-back, so the verification always fails and the ingress claim is aborted before any Item is created or deleted. This is **not** an artifact of this test's setup — it reproduced identically against a fresh, unmodified compendium `Gold Pieces` Item on a second attempt, and against a different coin (`Silver Pieces`) on a third.

**Impact.** PF2e's native currency is coin-denomination Treasure Items, not a flat currency object — so this defect makes it categorically impossible to move PF2e coins into or out of the Quartermaster vault via the normal drag-and-drop transfer flow. It does **not** affect the Currency Manager's display of existing coin totals, and it does not affect non-Treasure Items that happen not to carry an undefined-valued field (the `Dagger` and the synthetic token's item both transferred cleanly, matching a compendium check that found zero undefined fields in the `Dagger`'s raw data). **No data loss or duplication occurs** — the module's create-first, claim-before-mutate design means the failure is caught before either document is touched, and world state was verified consistent (source item and coin totals unchanged) after every failed attempt.

**Suggested fix direction (not implemented as part of the original verification):** either strip/normalize `undefined`-valued keys from both sides of the `writeVerifiedLog` comparison before the deep-equality check (e.g., round-trip the "intended" value through the same `JSON.parse(JSON.stringify(...))` normalization already used elsewhere in this file for `clone()`), or compare against a second `getFlag` read taken through the same normalization the retained value already receives.

**Fix landed and re-verified — 2026-08-10, against commit `7d9b55f9613e76444a600de4d162ccd1bd9893b5`.**

The fix (commit `4ce4706`, carried into `7d9b55f`) filters `undefined`-valued object keys out of `stableStringify`'s comparison before the deep-equality check — exactly the class of fix suggested above. `scripts/transaction-log.js`'s `stableStringify` now reads `Object.keys(value).filter(key => value[key] !== undefined).sort()` instead of taking all keys unconditionally, so a key that Foundry's actual persistence silently drops (because its value is `undefined`) is no longer compared against a "phantom" key on the intended-write side.

Re-verification used the same isolated world (`quartermaster-v14-pf2e-isolated`, reused, license already activated, not re-activated) with the fixed candidate's module freshly re-extracted from `artifacts/system-agnostic/module.zip` (SHA-256 `797531b86f26d3ca72285eb0b4302d77b70d14874e8bd4d6c142d78f5f71dd1a`, verified before and after extraction) into `Data/modules/quartermaster`. The presence of the fix in the running module was confirmed directly by fetching `scripts/transaction-log.js` from the live server and finding the `value[key] !== undefined` filter.

**Re-verification exercise (deliberately using a genuinely-reproducing Item, not the previous session's already-materialized coins).** The Test Hero PF2e Actor's existing `Gold Pieces`/`Silver Pieces` (created via `actor.inventory.addCoins` in the original setup) turned out to already carry a *defined* `system.price.sizeSensitive: false`, not `undefined` — those specific items would not have exercised the bug either way. To make sure the fix was tested against the *exact* originally-reported failure mode, a fresh, completely untouched `Gold Pieces` Item was pulled straight from the `pf2e.equipment-srd` compendium and inspected: its raw source (`toObject()`) confirmed `system.price.sizeSensitive` present as a key but `undefined` in value — the precise repro condition documented in the original failure writeup above.

- **Linked owned Actor (Test Hero PF2e).** The pre-existing `Gold Pieces` Item (quantity 15, `sizeSensitive: false`, defined) was dragged from the Actor's own sheet onto the `Shared Party Inventory` drop zone (a real `DragEvent`/`DataTransfer` carrying the standard Foundry `{type: "Item", uuid: ...}` payload was dispatched at the popup's registered drop zone, invoking the module's actual `onPopupDrop` handler exactly as a physical drag would). Result: `Gold Pieces added to Quartermaster Vault.`, no error. The transaction log recorded a full `claim` → `commit` pair (`transfer.ingress.claim`, `transfer.ingress.commit`, `matchesPredictedId: true`), the source Item was removed from the Actor, and a new Item (same quantity, same price data) existed on the vault. The vault Item was then given back to the Actor via the module's own `transferInventoryItemToActor` (the same function the popup's item-row "Add to Actor" context-menu option calls) — egress also succeeded cleanly (`Gold Pieces given to Test Hero PF2e.`), with the Item fully restored to the Actor and removed from the vault.
- **Unlinked Token Actor (Synthetic Token Source).** A fresh, untouched compendium `Gold Pieces` Item (the genuine `sizeSensitive: undefined` case, quantity 20) was created directly on the token's synthetic Actor, then dragged onto the vault the same way, using its `Scene.<id>.Token.<id>.Actor.<id>.Item.<id>` UUID. Result: `Gold Pieces added to Quartermaster Vault.`, no error — this is the exact scenario that failed before the fix. The Item was then transferred back to the token's synthetic Actor via `transferInventoryItemToActor`; egress succeeded cleanly, restoring the full quantity to the token Actor with no residue left in the vault.
- **No `canonical-transaction-log-verification-failed` error occurred in any of the four transfers** (two directions × two Actor types), against the true repro condition.
- **Reload/restart/reconnect spot-check.** The Foundry client was fully navigated away and the GM re-logged in (equivalent to a client reload/reconnect). `Quartermaster | v1.0.0 loaded` appeared cleanly with no errors. All post-transfer state was confirmed intact and correctly persisted: Test Hero PF2e held `Silver Pieces` (5) and `Gold Pieces` (15); the unlinked token's synthetic Actor held `Splatrope Extruder` and `Gold Pieces` (20); the vault held only the pre-existing `Dagger` — no orphaned, duplicated, or lost Items anywhere.
- Load/value/quantity, player privacy, hostile-request rejection, and hide/reveal were not re-run in full this session (per task scope, since nothing in this narrowly-scoped fix touches those code paths and the prior session's evidence above already covers them); the transaction log inspected during this session still showed the prior session's `source-actor-not-owned` and `invalid-transfer-boundary` hostile-rejection entries intact and unaffected.

**This closes the previously-open defect. The Pathfinder 2e 8.4.0 cell is now Passed in full**, with no known defects.

## v14.365 / Custom System Builder 6.0.2 evidence — Failed (one defect fixed and re-verified, one still open)

Tester: automated verification session. Date: 2026-08-10. Platform: Windows 11, Foundry v14 Build 365, headless `node main.js`, isolated data path `test-data/v14-dnd5e` (world `quartermaster-v14-csb-isolated`, created fresh for this cell).

**Setup required.** Installed "Custom System Builder" (LinkedFluuuush, v6.0.2, verified for 14.365) via the package browser — no login required. World creation initially failed ("The World could not be created as required dependencies were not installed") because Foundry's dependency dialog listed two auto-installable dependencies (`_chatcommands` 2.0.6, `statuscounter` 3.1.2) as merely "optional" but still gated world creation on their presence; a third, manual-only dependency (`dfreds-convenient-effects`) has no version compatible with this Foundry/system combination and was left uninstalled since it was not required to proceed. Installing the two auto-installable dependencies allowed world creation to complete. Because Custom System Builder is a blank template-authoring system with no actor types until the GM configures one, and no built-in Quartermaster adapter, enabling the module surfaced Quartermaster's own **"Confirm Storage Type"** dialog ("Custom System Builder does not have a built-in Quartermaster adapter. Choose the Actor type Quartermaster should use for its private and shared storage.") — this is exactly the minimal, module-provided setup step the release-gate anticipated for an unsupported system. `character` (CSB's only real actor type) was selected and storage was created successfully, confirming the generic-adapter fallback path (`GENERIC_CURRENCY_ID`, `qm-cur-default`) is reachable and functional for a genuinely unknown system.

**Passed checks:**

- Generic-currency exercise: the `Manage Currencies` → `Add Currency` quick-add flow (10 `CUR`) correctly mutated and persisted the vault's `CUR` balance; the inventory panel correctly showed `10 CUR` with no native-currency UI (matching the generic adapter's declared `nativeCurrencies: false, load: false` capabilities).
- Sidebar suppression for both GM and Player2.
- Hostile-request rejection: a same-Actor/same-boundary request was rejected `invalid-transfer-boundary`; a request sourced from an unowned Actor was rejected `source-actor-not-owned`. Both rejections happen at client-side/coordinator validation, before the flawed log-write step below, so they are unaffected by either defect.
- Hide/reveal: a vault-resident Item (created directly via the panel's `Create Item` button, which does not go through the transfer pipeline) was hidden to staging, confirmed invisible to Player2, and revealed back to the shared vault correctly.
- Reload/restart/reconnect: both GM and Player2 clients survived a full server-process restart with a clean Quartermaster init sequence, correct sidebar suppression, and persisted vault contents (Item and `CUR` balance both intact).

**Failed check 1 — Item ingress, same root cause as the Pathfinder 2e defect, but affecting every Item.**

Dragging a freshly created CSB Item ("Test Widget", type `equippableItem`, no unusual configuration) from a linked Actor onto the vault fails identically: `canonical-transaction-log-verification-failed`. Inspecting the Item's raw data found four `undefined`-valued keys inherent to Custom System Builder's own item template schema: `system.templateSystemUniqueVersion`, `system.uniqueId`, `system.template`, `system.container`. Because these fields are part of CSB's core item-template machinery, they are present (and `undefined`, pending a template being assigned) on essentially every CSB Item — meaning this is not a corner case for this system, it is the default state, and **Item ingress via drag-and-drop into the Quartermaster vault does not work at all for Custom System Builder**, for the same root cause documented above. Direct Item creation via the panel's own `Create Item` button (which bypasses the transfer/claim pipeline) is unaffected and works normally.

**Failed check 2 — a second, distinct defect: CSB's Actor sheet does not honor Quartermaster's egress interception.**

Testing egress by simulating a drag of a vault-resident Item onto a CSB Actor sheet produced an unexpected result: the Actor received a copy of the Item (retaining the *same* document ID as the vault original, indicating `keepId: true` semantics), but the vault's copy was **not removed** — instead a **second, new-ID copy** of the same Item appeared in the vault afterward. Querying `api.transactionLogQuery` found **zero** `transfer.egress.*` log entries anywhere in the log for this operation. Combined, this means: Quartermaster's own `submitEgressRequest` → `performTransfer` pipeline never ran (or exited before any log write) for this drop, yet an Item was still created on the destination Actor with the source's ID preserved — behavior consistent with Custom System Builder's own Actor-sheet drop handling processing the drop natively and independently of Quartermaster's `Hooks.on("dropActorSheetData", ...)` interceptor, which is supposed to `return false` and suppress exactly this default behavior (see `scripts/drag-drop.js`, `registerEgressInterceptor`). The net effect is an **unaudited, untracked Item duplication** with no transaction-log record and no confirmation that the vault-side deletion Quartermaster's pipeline would normally perform ever happened. This is a second, independent defect from the ingress failure above and should be investigated and fixed separately — it represents a real integrity/audit gap for this specific system, not merely a UX inconvenience. It was not further characterized (e.g., whether it reproduces on every egress attempt or only under specific conditions) due to session time constraints; a focused follow-up session should reproduce it deliberately and trace CSB's own sheet drop-handling code to confirm the mechanism.

**Impact (at the time of the original failure).** Combined with the ingress defect, Custom System Builder support was not usable for its core purpose (moving Items through the vault); only the generic-currency path and direct panel-side Item creation/hide/reveal were unaffected.

**Failed check 1 — re-verified fixed, 2026-08-10, against commit `7d9b55f9613e76444a600de4d162ccd1bd9893b5`.**

The same `stableStringify` undefined-key filter that fixed the Pathfinder 2e defect (see above) also fixes this one — both defects shared the identical root cause. Re-verification used the same isolated world (`quartermaster-v14-csb-isolated`, reused) with the fixed candidate's module freshly re-extracted (SHA-256 `797531b86f26d3ca72285eb0b4302d77b70d14874e8bd4d6c142d78f5f71dd1a`, verified before and after extraction), and the fix's presence in the running module was confirmed the same way (fetched `scripts/transaction-log.js` from the live server, found the `value[key] !== undefined` filter).

The `Test Hero CSB` Actor's `Test Widget` Item was inspected first and confirmed to still carry all four `undefined`-valued keys documented in the original failure (`system.templateSystemUniqueVersion`, `system.uniqueId`, `system.template`, `system.container` all present as keys, all `undefined` in value) — this is CSB's default per-Item state, unaffected by the module fix, so it remains a faithful repro condition.

- **Linked owned Actor (Test Hero CSB), ingress via popup drag.** `Test Widget` was dragged from the Actor's own sheet onto the vault's drop zone (real `DragEvent`/`DataTransfer` dispatch, invoking the actual `onPopupDrop` handler). Result: `Test Widget added to Quartermaster Vault.`, no error. The Item was removed from the Actor and a new Item (all four undefined keys, unchanged) existed on the vault.
- **Linked owned Actor, egress via popup's own "Add to Actor" action** (`transferInventoryItemToActor`, the function the popup's item-row context menu calls). Result: success, Item fully restored to the Actor and removed from the vault.
- **Unlinked Token Actor (`Synthetic Token Source CSB`), ingress via popup drag.** The token's synthetic Actor's `Token Gadget` Item was dragged onto the vault the same way, using its `Scene.<id>.Token.<id>.Actor.<id>.Item.<id>` UUID. Result: `Token Gadget added to Quartermaster Vault.`, no error.
- **Unlinked Token Actor, egress via `transferInventoryItemToActor`.** Result: success, Item fully restored to the token's synthetic Actor.
- **No `canonical-transaction-log-verification-failed` error occurred in any of the four transfers.** The general Item-transfer defect is confirmed fixed for Custom System Builder, matching the Pathfinder 2e result.

**Failed check 2 — re-reproduced, and the exact mechanism has now been identified. Still open; not fixed in this session.**

The native-sheet-egress-duplicate scenario was re-attempted precisely as described in the task: an Item was dragged from Quartermaster's inventory popup (`Vault Widget`, itemId `QbanFWMAtAzW6jLL`, using the real `qmEgressDrag`-marked `DataTransfer` payload the popup's own item rows set on `dragstart`) and dropped directly onto the CSB Actor's own rendered sheet (`CharacterSheetV2-Actor-<id>`, not the popup). Result, confirmed by direct inspection of world state before and after:

- A **new** Item (`ofc8WW18h2Yjw8JT`, name `Vault Widget`) was created directly on the Actor, in addition to the Actor's pre-existing `Vault Widget` (`iljfRXKISdXcipop`) — a duplicate.
- The vault's `Vault Widget` (`QbanFWMAtAzW6jLL`) was **not removed** — it remained in the vault, untouched.
- `api.transactionLogQuery.queryEntries()` showed **zero** new entries for this operation — the most recent entries were from an unrelated transfer several seconds earlier. No `transfer.egress.claim`, no `transfer.egress.commit`, nothing.
- No `Quartermaster |` console messages of any kind were logged for this drop — confirming Quartermaster's interception code never ran at all. (Earlier in this same session, a genuine PF2e sheet-drop egress went through the `dropActorSheetData`-hook path and produced full claim/commit log entries; PF2e's egress in this session was exercised via the popup's own `transferInventoryItemToActor` action rather than a raw drag onto its native sheet, so this comparison is by code-path design, not a directly observed side-by-side console diff.)

**Root cause, pinned down precisely this session.** Foundry's core `ApplicationV2`-derived `DocumentSheetV2._onDrop` (in the client bundle, `scripts/foundry.mjs`) does call `Hooks.call("dropActorSheetData", actor, this, data)` — so the hook mechanism itself is sound, and ApplicationV2 sheets are not categorically incompatible with it (this is confirmed by Pathfinder 2e's `CharacterSheetPF2e`, which is also an `ApplicationV2`/`DocumentSheetV2` subclass in this Foundry version, and correctly fires the hook). Custom System Builder's own sheet class, however, does not inherit this behavior. Reading `Data/systems/custom-system-builder/module/sheets/actors/v2/CharacterSheetV2.js` (installed system source, read-only; not part of the protected paths) shows:

- `CharacterSheetV2` builds its **own** drag-drop wiring in its constructor using `foundry.applications.ux.DragDrop.implementation`, with `dropSelector: null` (meaning the drop callback binds to the whole sheet element) and `callbacks: { drop: this._onDrop.bind(this) }` (lines 54–76).
- It defines its **own** `async _onDrop(event)` method (lines 132–146) that parses the drag data directly (`TextEditor.implementation.getDragEventData(event)`) and, for `type === "Item"`, calls `this._onDropItem(event, item)` directly.
- **This override never calls `super._onDrop(event)` and never calls `Hooks.call("dropActorSheetData", ...)` anywhere.** It goes straight to `_onDropItem` → `_onDropItemCreate` → `CustomItem.createDocuments(..., { keepId: true })`, which is exactly why the dropped Item lands on the Actor with a *new* auto-generated ID (not the vault's original ID — confirmed above: `QbanFWMAtAzW6jLL` in the vault vs. `ofc8WW18h2Yjw8JT` on the Actor) and why Quartermaster's interceptor — which only ever runs from inside the `dropActorSheetData` hook callback — never gets a chance to run, intercept, or log anything.
- No `dropActorSheetData` string or `Hooks.call`/`Hooks.callAll` reference of any kind exists anywhere in the installed Custom System Builder system source (confirmed by a full-tree grep).

In short: this is not a hook *naming* mismatch, a missing registration, or an ApplicationV2-vs-V1 incompatibility in general — it is that Custom System Builder's specific sheet class replaces the entire drop-handling pipeline with its own, bypassing the point in the base class where the hook would have been called.

**Why no fix was implemented this session.** A generic (non-CSB-named) fix is architecturally possible: since the browser dispatches `drop` DOM events in capture phase from `document` down to the target *before* any element-level listener (including CSB's own `DragDrop`-bound listener) runs, Quartermaster could add a `document.addEventListener("drop", handler, true)` capture-phase listener that (a) only acts when the event's `dataTransfer` carries Quartermaster's own `qmEgressDrag`/`qmReHideDrag` markers — meaning it is provably inert for every drag Quartermaster did not itself initiate, so it cannot affect unrelated drops (canvas, hotbar, journal, compendium, etc.); (b) resolves the destination Actor generically by walking up from `event.target` through `foundry.applications.instances` (Foundry's own core ApplicationV2 registry, keyed by the same `id` attribute every ApplicationV2 sheet's root element carries — confirmed live, not CSB-specific); and (c) if a destination Actor is found, calls `preventDefault()`/`stopImmediatePropagation()` and runs the same `handleEgressDrop`/`handleLootPrepToSheetDrop` logic the existing hook callback already uses. This satisfies the task's constraint of not special-casing `"customsystembuilder"` by name and would, in principle, close the gap for CSB and any other system whose sheet class bypasses the hook the same way.

It was not implemented because: (1) `scripts/drag-drop.js` currently has **zero existing unit test coverage** (no `tests/*drag*` file exists at all), so shipping this change with real test coverage — as the task requires ("implement and unit-test it") — means building non-trivial new mocking infrastructure for `document`, `DragEvent`/`DataTransfer`, and `foundry.applications.instances` from scratch in the same session, which could not be done with the same rigor as pinning down the diagnosis itself; and (2) a document-level capture-phase interceptor, even one narrowly gated on Quartermaster's own drag markers, changes the *primary* interception mechanism for every system this module supports (it always wins the propagation race, so it would take over from the existing `dropActorSheetData` hook path even for systems — D&D 5e, Pathfinder 2e — where that path is already confirmed working), and verifying that substitution doesn't regress those already-passing cells would need broader live regression testing than this session's remaining scope allowed. Per the task's explicit instruction not to ship a fix without confidence in its safety, this is left as a precisely-documented, reproduced, root-caused, still-open defect for a dedicated follow-up session with room to build the missing test infrastructure and re-run the full live matrix afterward.

**Impact.** The general Item-transfer defect that made Custom System Builder unusable for its core purpose is fixed and re-verified. The remaining defect is narrower than originally scoped: it only affects egress performed by dragging directly onto CSB's native Actor sheet (bypassing Quartermaster's own popup UI, which most players/GMs would not normally do since Quartermaster's own inventory popup is the intended interface) — ingress and egress performed through Quartermaster's own popup (drag-to-popup, or the popup's own "Add to Actor" action) are unaffected and work correctly. It remains a real audit/integrity gap: a duplicate, untracked Item can be created with zero log trail if a user drags directly onto the native sheet.

**Milestone 2 fix landed and live-verified (2026-08-24).** `scripts/drag-drop.js` registers a capture-phase document `drop` listener gated exclusively on Quartermaster drag markers (`qmEgressDrag`, `qmLootPrepDrag`). It resolves destination Actors via `foundry.applications.instances`, routes through the existing audited `handleEgressDrop` / `handleLootPrepToSheetDrop` paths (including client preflight and `submitRequest`), dedupes against the `dropActorSheetData` hook, and calls `preventDefault` / `stopImmediatePropagation` only for recognized Quartermaster egress.

Headless coverage: six tests in `tests/drag-drop.test.mjs`; full suite **114/114** on the dirty tree (2026-08-24, includes `query-sender` unit tests).

Live isolated cell: Foundry **v14.367.0.0** (`FoundryVTT-Runtimes\v14.367`), data path `Quartermaster\test-data\v14-dnd5e`, port **30001**, world `quartermaster-v14-csb-isolated`, system Custom System Builder **6.0.2**. Isolated module SHA-256 `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD` matches the dirty worktree (`scripts/drag-drop.js` and siblings including `query-sender.js`, `mutation-rate-limit.js`, `socket-handler.js`).

**CSB re-run on interceptor `51E7246F…` (2026-08-24, this session).** Marked native-sheet egress via `handleDocumentCaptureDrop` onto `CharacterSheetV2-Actor-mN0ajcM3rvrWHsqZ` transferred one copy of `Live P2 Auth Probe`, emptied the vault source, and fired `preventDefault` / `stopImmediatePropagation` with `foundry.applications.instances.has` true (`ui.windows` match false). Prior B3B9DD8C… evidence is superseded for CSB native-sheet egress on this interceptor.

Prior session evidence on interceptor `B3B9DD8C…` (popup ingress/egress, unlinked Token Actor UUID ingress/egress, Player2, process-restart on CSB) still stands where not contradicted above; hero inventory retains duplicate `Vault Widget` items from earlier pre-fix probes.

**Current dirty-interceptor v14.367 matrix (2026-08-24).** Same isolated data root; do not treat older v14.365 committed-candidate rows as a pass for this interceptor. After PF2e live evidence showed `CharacterSheetPF2e` registered only in `ui.windows` (not `foundry.applications.instances`), `scripts/drag-drop.js` now resolves marked drops against ApplicationV2 instances first, then legacy ApplicationV1 windows by matching the walked DOM id to `sheet.id`. Unmarked drops remain inert. Isolated install SHA-256 `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD` matches the dirty worktree.

| Foundry | System | Status | Notes |
| --- | --- | --- | --- |
| 14.367 | Custom System Builder 6.0.2 | **Passed** on interceptor `51E7246F…` | Native-sheet capture egress re-verified this session (`preventDefault`/`stopImmediatePropagation`, single copy, vault emptied). Prior popup/token/Player2/reload/restart evidence on `B3B9DD8C…` stands; hero duplicate `Vault Widget` pollution unchanged. |
| 14.367 | D&D 5e 5.3.3 | **Passed** on interceptor `51E7246F…` | Prior linked/token interceptor, native GP, hostile reject, reload, Player2, public API, loot-prep reveal still stand. **Process-restart persistence passed** this session: kill+relaunch isolated v14.367 exe/`dataPath`/`world=quartermaster-v14-dnd-isolated`, GM rejoin; vault (`Dagger` `scn0Wzys1WTgI8AZ`), staging (leftover `Dagger` `4EJcqTZkBezMfDnR`), hero items, and GP (10) identical. Staging leftover is probe pollution, not a restart failure. **Rate-limit spot-check passed** (`max=1` → second `CURRENCY_CHANGE` `rate-limited`, GP +1 only). **Query-sender sanity passed**: v14 `CONFIG.queries` present; `resolveAuthenticatedQuerySender` accepts `options.user`; conflicting fake `userId` without `options.user` fails closed (`null`); GM local `submitRequest` succeeds. |
| 14.367 | PF2e 8.4.0 | **Passed** (item transfers on interceptor `51E7246F…`) | Compendium `Dagger` (`weapon`, not coins). Popup ingress moved the probe off Test Hero PF2e onto the vault. Marked native-sheet egress on `CharacterSheetPF2e-Actor-Ma0yCu5nqH7LKFjP` fired `preventDefault` / `stopImmediatePropagation` with `ui.windows` match (`instancesHas` false). Unlinked Token Actor UUID `Scene.NUEDEFAULTSCENE0.Token.pqVOQMkWeHVzABrM.Actor.4LcoadqIWbk2Gj4S` ingress/egress passed. Unmarked native drops stayed inert. Vault already contained extra Daggers from earlier failed probes. |

**Current dirty-interceptor v13.351 matrix (2026-08-24).** Isolated `test-data\v13-351-dnd5e`, Foundry 13.351, port **30011**, interceptor SHA-256 `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD`.

| Foundry | System | Status | Notes |
| --- | --- | --- | --- |
| 13.351 | D&D 5e 5.3.3 | **Passed** including Player2, process-restart, and rate-limit | World `quartermaster-v13-dnd-isolated`. Prior interceptor checks stand. Player2: unowned token egress `destination-actor-not-owned`, staged grab `invalid-transfer-boundary` with no mutation, owned hero→vault ingress succeeded. Process-restart: vault/staging/hero/GP identical after kill+relaunch of the isolated `--dataPath` process. Rate-limit: GM `CURRENCY_CHANGE` with world setting max=1 returned `rate-limited` on the second request; settings restored to 30/10000. Foundry v13.351 does not pass `options.user` into `CONFIG.queries`; identity is taken from the server-authenticated `userQuery` sender id. Extra Gamemaster Chrome sessions can ack first with a stale handler — live Player2 checks require a single GM client. |
| 13.351 | PF2e 7.12.2 | **Passed** | Isolated `test-data\v13-351-pf2e`, port **30012**, world `quartermaster-v13-pf2e-isolated`. Full minimum matrix on interceptor `51E7246F…` including coin Items, GM hostile, hide/reveal, client reload; prior token/restart/rate-limit/Player2/interceptor checks stand. |
| 13.351 | CSB 5.2.1 | **Passed** | Isolated `test-data\v13-351-csb`, port **30013**, headless, world `quartermaster-v13-csb-isolated`. Full minimum matrix on interceptor `51E7246F…` including token UUID, GM hostile, hide/reveal, client reload, generic CUR UI; prior restart/rate-limit/Player2/interceptor checks stand. |

**v13.351 PF2e 7.12.2 / CSB 5.2.1 provisioning evidence (2026-08-24).** Worktree `47347b1` on `feat/system-agnostic`. Manifest versions verified before install: PF2e **7.12.2** (`https://raw.githubusercontent.com/foundryvtt/pf2e/v13-dev/system.pf2e.json`), CSB **5.2.1** (`https://gitlab.com/custom-system-builder/custom-system-builder/-/raw/5.2.1/system.json`). New isolated cells created under `Quartermaster\test-data` with explicit `--dataPath`, `--noupnp`, `--noupupdate`. License keys copied from existing isolated cell `v13-351-dnd5e\Config\license.json` (required before worlds launch on fresh data paths). Verified process cmdlines: PF2e PID on `--dataPath=...\v13-351-pf2e --port=30012 --world=quartermaster-v13-pf2e-isolated`; CSB PID on `--dataPath=...\v13-351-csb --port=30013 --world=quartermaster-v13-csb-isolated --headless`. Module tree copied excluding `.cursor`; `drag-drop.js` SHA-256 `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD` on both cells. Live checks via disposable Chrome CDP scripts under each cell directory (not committed). PF2e matrix output: `POPUP_INGRESS pass`, `NATIVE_SHEET_EGRESS pass` (`preventDefault:1`, `stopImmediatePropagation:1`), `UNMARKED_DROP_INERT pass`. CSB matrix output: same three checks pass after storage bootstrap + client reload. Remaining minimum-matrix checks completed in follow-up session — see sections below.

**v13.351 PF2e / CSB session follow-up evidence (2026-08-24, same interceptor).** Single GM Chrome per world required for v13 query-sender ack routing (duplicate GM tabs on 30012 caused empty CDP results until extra profile on debug port 9240 was stopped). **PF2e 7.12.2 — token UUID transfer passed:** orphan token cleaned; fresh unlinked token `Synthetic Token Source` (`actorLink: false`, UUID `Actor.anSXUDWb4duMMufQ`); compendium equipment **Dagger** (not coins) popup ingress and marked native-sheet egress with `preventDefault:1` / `stopImmediatePropagation:1`. **PF2e — rate-limit passed:** `mutationRateLimitMax=1`; first `CURRENCY_CHANGE` (`gp`) success; second `rate-limited`; vault gp 0→1 only; settings restored to 30/10000. **PF2e — Player2 passed:** unowned token egress `destination-actor-not-owned`; staged grab `invalid-transfer-boundary` with no mutation; owned hero Dagger→vault ingress `success`. **PF2e — process-restart passed:** kill+relaunch isolated `--dataPath` process; vault (`Gold Pieces`, `Dagger`), staging (`Dagger`), hero items, and gp (1) identical before/after. **CSB 5.2.1 — rate-limit passed:** `qm-cur-default` reference currency; max=1 → second request `rate-limited`; CUR 0→1 only; settings restored. **CSB — process-restart passed:** vault (`CSB Owned Probe`), staging (`CSB Staging Probe`), hero (`CSB Ingress Probe`), CUR (1) identical after headless kill+relaunch. **CSB — Player2 passed** (retry with vault item + unlinked token prep): `destination-actor-not-owned`, staged `invalid-transfer-boundary`, owned `CSB Owned Probe` ingress success. **CSB operator note (P2, not P0):** first boot on generic adapter still requires `quartermaster.vaultActorType=character` when no headed DialogV2 is available — document for operators; no product rewrite attempted this session.

**v13.351 PF2e / CSB remaining minimum-matrix evidence (2026-08-24, interceptor `51E7246F…`).** Single GM Chrome per world (`chrome-v13-pf2e-matrix` port 9232, `chrome-v13-csb-matrix` port 9233); extra probe Chrome sessions on other debug ports stopped before runs. Verified Foundry cmdlines include `--dataPath` under `Quartermaster\test-data`, `--noupnp`, `--noupupdate` (CSB also `--headless`). Live module SHA-256 confirmed in-browser: `51E7246FE2CB008F9FC443B577CE665B2C81866DFEF31BD16B83CAE0A154FDCD`.

**PF2e 7.12.2 — remaining checks passed (port 30012).**

- **Native coin Item transfer:** Fresh compendium `Gold Pieces` (`pf2e.equipment-srd`) on `Test Hero` with `system.price.sizeSensitive === undefined` (true repro condition). Popup drag ingress succeeded; egress via `transferInventoryItemToActor` from `drag-drop.js` returned `success`. No `canonical-transaction-log-verification-failed`.
- **GM hostile request:** Same-actor/same-boundary ingress on vault item rejected `invalid-transfer-boundary`; vault item count unchanged (4→4), source item still present.
- **Hide/reveal (real UI):** `handleInventoryGMAction('hide-item', …)` moved `Matrix Hide Probe` to staging; Loot Prep app opened and `[data-action="reveal-item"]` click restored item to vault.
- **Client reload:** Full page reload + GM rejoin; Quartermaster active, vault/staging item lists and gp (2) identical; inventory popup opened successfully.

**CSB 5.2.1 — remaining checks passed (port 30013).**

- **GM hostile request:** Same-actor/same-boundary ingress on `CSB Hostile Probe` rejected `invalid-transfer-boundary`; vault count unchanged.
- **Hide/reveal (real UI):** `CSB Matrix Hide Probe` hidden to staging via inventory GM action; Loot Prep reveal button restored to vault.
- **Unlinked token UUID transfer:** Reused valid unlinked token `Synthetic Token Source` (`Scene.NUEDEFAULTSCENE0.Token.Wcd1s1Qyv3Z50qGW.Actor.9rd11PNJEpwapR1C`, `actorLink: false`); `CSB Token Gadget` popup ingress + marked capture egress (`preventDefault:1`, `stopImmediatePropagation:1`). Prior broken token (`Synthetic Token Source CSB` with null actor) deleted before test.
- **Generic currency UI:** `CURRENCY_CHANGE` +5 on `qm-cur-default` succeeded (1→6); distinct from rate-limit probe.
- **Client reload:** Full page reload + GM rejoin + storage bootstrap; Quartermaster active, vault/staging/CUR (11) intact; inventory popup opened.

Disposable CDP scripts: `test-data\v13-351-pf2e\v13-pf2e-remaining.mjs`, `test-data\v13-351-csb\v13-csb-remaining.mjs` (not committed).

## Minimum live checks for every matrix cell

- Start Foundry with only the required system, Quartermaster, and unavoidable dependencies enabled.
- Create or select compatible storage Actors and open the shared inventory and GM staging interfaces.
- Transfer a complete Item into and out of the vault using both an owned linked Actor and an unlinked Token Actor; confirm source and destination state and the transaction log.
- Exercise the system's native currency path when supported, or the generic `Currency` (`CUR`) balance otherwise.
- Confirm load, value, quantity, and identification fields appear only when the adapter supplies accurate data.
- Confirm a player cannot read private staging Items, notes, canonical logs, or recovery records.
- Submit hostile ownership, direction, same-boundary, and storage-boundary requests; confirm they are rejected without mutation.
- Hide and reveal a representative Item; confirm it moves between shared and private Actors and remains invisible to the player while staged.
- Reload the client, restart the server/world, reconnect a client, and confirm persisted data and module startup remain healthy.
- Record pass/fail, tester, date, platform, clean-console observations, and any deviations in the matrix.

## Final evidence required before approval

1. The final commit identifier and a clean working tree.
2. Complete output from `npm test` on that commit.
3. A validated `artifacts/system-agnostic/module.zip` built from that commit, with its checksum and inspected file list.
4. Results for all six live matrix cells.
5. A completed live migration report for a disposable v0.1.8 world clone or representative synthetic fixture.
6. A dated `1.0.0` changelog entry reviewed against the final diff.
7. Separate, explicit authorization for merge, tag creation, manual installation, and publication.

Automated headless tests support this decision but do not substitute for the live Foundry matrix or migration rehearsal.
