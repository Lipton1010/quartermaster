# Quartermaster 1.0.0 Migration Report

## Status

**Live re-rehearsal on 2026-08-10 against commit `7d9b55f9613e76444a600de4d162ccd1bd9893b5` PASSED.** The original live rehearsal on 2026-08-10 against commit `6237480` had FAILED; two fixes landed afterward — commit `523f78801727c283e8c317f275483c766d7e3eac` (normalize primitive-only array fields, e.g. dnd5e's `properties` Set, to deduplicated/order-independent content before the per-item write-verification comparison) and commit `4ce4706` (a related `stableStringify` fix so write-verification tolerates benign `undefined`-valued object keys that Foundry silently drops on persist). This re-rehearsal repeated the full live-world methodology end to end against the fixed candidate — package checksum `797531b86f26d3ca72285eb0b4302d77b70d14874e8bd4d6c142d78f5f71dd1a` — and the automatic first-GM-startup migration **completed successfully**, with every checklist item confirmed by direct inspection of both storage actors, including a genuine duplicate-tolerant `properties: ["gear"]` Set field on every migrated Item (not artificially forced — this is dnd5e 5.3.3's normal document-preparation behavior) and a fractional (2.5 gp) hidden-currency entry. Idempotency held across both a client reload and a full Foundry process restart (`migrationState.attempts` stayed at `1`, item counts identical, no duplicates). See "Live rehearsal record" and "Authorized rehearsal result" below for full evidence, including the prior FAIL run's detail (kept for the record) and the new PASS run's detail.

The migration gate is now open for this candidate, pending human review of this report. This report does not approve migration of a production world by itself; always back up first. Never test against the only copy of a world or Foundry data directory.

## Migration under review

The v1 migration separates player-readable shared storage from GM-only staging storage. It is intended to copy and verify private legacy data before removing the legacy values, and to advance the storage-schema marker only after the complete migration succeeds.

The review must cover migration from the published v0.1.8 data shape to storage schema 1, including:

- hidden Items and their folder and note flags;
- loot-prep notes and folder assignments left on already-visible Items;
- staged currency and loot-prep folders;
- canonical and player-readable transaction-log projections;
- visible Items, native and custom balances, exchange configuration, approval settings, resources, user/world settings, and shortcut-token links that should remain available;
- safe restart after an interrupted or failed migration.

## Automated evidence available

On 2026-08-10, the pre-fix candidate (`6237480bca9ba9d69116216d30da731dbce604ec`) completed `npm test` with syntax checks for 79 JavaScript files, all 86/86 tests passing, and the release/system-boundary validator passing. The migration coverage demonstrated successful copy-and-verify behavior, idempotent reruns, staging-marker repair, scrubbing of loot-prep notes/folders left on already-visible legacy Items, detection of migrated fractional hidden-currency amounts that a whole-units-only native currency cannot reveal, and recovery after a simulated deletion failure without advancing the shared schema marker prematurely. (This is historical, pre-fix evidence, kept for the record — see the final candidate's current numbers in [CHANGELOG.md](../CHANGELOG.md)'s Verification section and [BUILD-REPORT.md](../../artifacts/system-agnostic/BUILD-REPORT.md): as of the final commit, 80 JavaScript files and 90/90 tests, including two new tests added specifically to cover the migration fix below.)

That run was headless and used synthetic Actor and Item doubles. It is exact-commit regression evidence accompanied by a validated package, but it is not a live Foundry migration result.

## Fresh-world live evidence (not a migration rehearsal)

An isolated Foundry v14.365 / D&D 5e 5.3.3 world successfully created schema-1 shared and private storage, preserved the privacy boundary during GM hide/reveal and Player2 access checks, and loaded its world databases to `Complete` after a process-level server restart. Browser safety policy prevented a client reconnection after that restart, so module client recovery was not established. Because the world began fresh rather than with v0.1.8 data, none of this proves legacy data preservation, migration idempotency, or interruption recovery in Foundry.

## Live rehearsal record

### Re-rehearsal (2026-08-10, candidate `7d9b55f9613e76444a600de4d162ccd1bd9893b5`) — PASSED

| Check | Status | Evidence |
| --- | --- | --- |
| Back up or clone a representative v0.1.8 world | Done (substitute) | Same substitution rationale as the original rehearsal (no real v0.1.8 world exists to clone; production campaign data is off-limits). A leftover module copy under `test-data/v14-migration/Data/modules/quartermaster` from a prior interrupted attempt was found to be corrupt — it was missing 13 `test-step*.js` files that `scripts/module.js` still imported, so the module reported `active` but every hook silently failed to register (0 console output, all `test-step*.js` requests 404). It was rebuilt clean from the read-only tag checkout via `git archive 14ee13c4efbcf9cfdfd71d5b71102d77b9e8b260`, confirmed all 48 files including the 13 `test-step*.js` present, and reloading the world showed the module fully active with a working `api` object. Foundry v14.365 was relaunched on the isolated `test-data/v14-migration` path, port 30002, and a fresh world `migration-rehearsal-v3` was created bound to D&D 5e 5.3.3 (the prior attempt's partially-built world was deleted per the task's explicit allowance). |
| Record pre-migration Items, balances, resources, folders, notes, logs, settings, and token links | Done | Populated and captured via the module's own public API on backing actor "Quartermaster Vault" (`3jO4rAAyrWYbJja1`): 5 total Items (3 hidden: Ruby Pendant, Silver Locket, Gemstone Cluster — 2 with loot-prep notes and a shared folder "Dragon Hoard Prep", 1 uncategorized; 2 visible: Masterwork Longsword carrying a deliberately leaked `lootPrepNote`/`lootPrepFolder` flag pair, and Traveler's Cloak with none); 1 loot-prep folder; 3 staged hidden-currency entries (2.5 gp fractional, 40 sp, 3 pp); native visible currency `pp2 gp85 ep0 sp30 cp12`; 1 custom resource "Rations (party)" 12/20; 3 transaction-log entries (`currency.deposit`, `hidden.staged`, `resource.update`). Every Item's `system.properties` field naturally contained `["gear"]` (dnd5e 5.3.3's own data-preparation default), giving the same Set-backed-field precondition that broke the prior candidate. |
| Mount the reviewed candidate in the disposable test world | Done | Foundry was stopped, `Data/modules/quartermaster` was replaced wholesale with the extracted contents of `artifacts/system-agnostic/module.zip` (SHA-256 verified `797531b86f26d3ca72285eb0b4302d77b70d14874e8bd4d6c142d78f5f71dd1a` both before and after extraction), and Foundry was relaunched. The Setup screen and a direct `fetch('/modules/quartermaster/module.json')` both confirmed version `1.0.0`. |
| Run first-GM-startup migration | **PASSED** | On GM login, console logged `Staging actor created: Quartermaster Staging`, `Storage actor ID drift detected; updating stagingActorId`, then `Storage migration to schema 1 complete` — no error, no verification failure. `migrationState` on the shared actor: `{status:"complete", phase:"complete", attempts:1, error:null, migratedItemCount:3, migratedVisibleItemMetadataCount:1, canonicalLogCopied:true}`. All 5 Items now show `properties:["gear"]` (single entry, not duplicated) on both actors — the exact duplicate-tolerant case the fix targets was exercised naturally and passed. |
| Verify shared storage remains player-readable | **PASSED** | Shared actor "Quartermaster Vault" ownership `default: 2` (OBSERVER). A dedicated non-GM user "Player2" was created and logged in via a second browser tab/session: `game.actors.filter(a=>a.visible)` showed only "Quartermaster Vault" in the directory; `shared.testUserPermission` / `.visible` = true. Player2 could read the shared actor's 2 visible Items (Masterwork Longsword, Traveler's Cloak — both with `lootPrepNote`/`lootPrepFolder` now `null`), native currency (`pp2 gp85 ep0 sp30 cp12`, unchanged), the custom resource (12/20, unchanged), and a redacted transaction log (2 of the 3 entries — the `hidden.staged` entry referencing a still-private Item was correctly excluded from the player-readable projection). |
| Verify private staging data is inaccessible to players | **PASSED** | Staging actor "Quartermaster Staging" ownership `default: 0` (NONE). As Player2: `staging.visible` = `false` and it was absent from `game.actors.filter(a=>a.visible)` (not shown in the sidebar directory); `staging.isOwner` = `false`. This matches the standard Foundry ownership/visibility gate the module relies on. Note for the record (not a defect, and not new to this fix): Foundry's client-authoritative sync model still loads full Actor/Item document data for all connected clients regardless of ownership — a player who opened devtools and called `game.actors.get(id)` directly could read the raw fields of a `visible:false` actor. This is a platform-wide characteristic of Foundry (true before and independent of this migration) and was also true, unaddressed, of the pre-fix build; it is out of scope for this migration fix and is noted here only for completeness. |
| Verify all expected data against the pre-migration inventory | **PASSED** | Staging actor holds exactly the 3 previously-hidden Items with their notes and folder intact (Ruby Pendant / Silver Locket retain their original note text and `folderId`; Gemstone Cluster remains uncategorized) plus all 3 hidden-currency entries including the 2.5 gp fractional one, unchanged, plus the loot-prep folder. `migratedItemMetadata` on staging records the scrubbed leak from Masterwork Longsword (`note`, `folderId` preserved for audit) even though the Item itself stayed on the shared actor. Canonical transaction log on staging has all 3 original entries (`canonicalLogCopied:true`); the shared actor's player-facing projection has the 2 non-sensitive entries. Native currency and the custom resource are unchanged on the shared actor. `storageSchemaVersion:1` set on both actors. Fractional-currency handling: `migrationState.fractionalCurrencyWarnings` is present as an (empty) array — correctly empty because dnd5e's currency adapter explicitly preserves decimal balances (only whole-unit systems like PF2e populate this warning), confirmed by reading `system-adapters/dnd5e.js` and `storage-migration.js`'s `detectFractionalHiddenCurrency`. |
| Interrupt and resume a disposable migration | Attempted, inconclusive | A second disposable world (`test-data/v14-migration-interrupt`, same v0.1.8→v1.0.0 swap procedure, checksum re-verified) was populated with one hidden Item and one fractional (1.5 gp) hidden-currency entry. A file-system watcher polled the world's `actors`/`items` LevelDB WAL files and force-killed the Foundry process the instant either grew after GM login. The process was confirmed killed (`Stop-Process -Force`, PID gone, port freed) roughly in the expected window, but the persisted state after relaunch showed `migrationState.attempts:1` and `phase:"complete"` with no error — i.e. the *entire* migration (which completes in roughly 300ms end-to-end, per `startedAt`/`completedAt` deltas observed in the primary rehearsal) had already finished and the shared schema marker had already advanced before the OS-level process termination actually took effect. A rerun afterward was clean (no duplicate Items, staging holds exactly 1 Item, no error), which is *consistent with* correct interruption-safe behavior but does not *prove* it, since no genuine mid-migration state was captured. Reliably interrupting a sub-second in-process migration via external process-kill timing, without adding test-only instrumentation to the candidate itself, was not achievable in this black-box rehearsal; this is a methodology limitation, not a finding about the candidate. |
| Restart Foundry and recheck persistence and idempotency | **PASSED** | Client reload (same process): console showed `Staging actor present` (not "created") and no re-run of `Storage migration...complete`; `migrationState.attempts` stayed `1`; item counts and hidden-currency entries unchanged. Full Foundry process restart (`Stop-Process`, confirmed port 30002 freed, relaunched, GM logged back in): same result — `attempts:1`, `phase:"complete"`, shared item count 2, staging item count 3, hidden-currency count 3, both `storageSchemaVersion:1`, no duplicates of any kind. Migration is fully idempotent across both a client reload and a full process restart. |

### Original rehearsal (2026-08-10, candidate `6237480bca9ba9d69116216d30da731dbce604ec`) — FAILED (kept for the record)

| Check | Status | Evidence |
| --- | --- | --- |
| Back up or clone a representative v0.1.8 world | Done (substitute) | No real v0.1.8 world exists to clone (the only live data is the user's production campaign, which is off-limits). A clean v0.1.8 module package was rebuilt read-only from tag commit `14ee13c4efbcf9cfdfd71d5b71102d77b9e8b260` via `git archive`, installed into a fresh, isolated Foundry v14.365 data path (`test-data/v14-migration`, port 30002), and used to create a synthetic legacy world (`migration-rehearsal`) bound to D&D 5e 5.3.3. |
| Record pre-migration Items, balances, resources, folders, notes, logs, settings, and token links | Done | Captured via the module's own public API (`game.modules.get('quartermaster').api`) on the single v0.1.8 backing actor "Quartermaster Vault" (`SsBbV5Zl3M8VFfeX`): 5 total Items (3 hidden, 2 visible); 1 loot-prep folder "Dragon Hoard Prep"; 3 staged hidden-currency entries including one fractional amount (2.5 gp, plus 40 sp and 3 pp); native visible currency `pp2 gp85 ep0 sp30 cp12`; 1 custom resource "Rations (party)" 12/20; 3 transaction-log entries. One visible Item ("Masterwork Longsword") was deliberately left carrying stray `lootPrepNote`/`lootPrepFolder` flags to simulate the pre-fix leak scenario. Full inventory JSON preserved for this rehearsal's record. |
| Mount the reviewed candidate in the disposable test world | Done | Foundry was stopped, `Data/modules/quartermaster` in the isolated data path was replaced wholesale with the extracted contents of the validated `artifacts/system-agnostic/module.zip` (SHA-256 verified to match `5e7f0b7eca6dfe9e63a272c669d02c964615107a6f12f6d3c40e8ffec384c76e` both before and after use), and Foundry was relaunched on the same isolated path/port. The Setup screen confirmed Quartermaster now reports version 1.0.0. |
| Run first-GM-startup migration | **FAILED** | On GM login, `ensureStorageActors` created a new private "Quartermaster Staging" actor and began `migrateStorageSchema`. It failed deterministically in the `items` phase with `hidden-item-verification-failed-9YWdBk75W65TiCHU` (the "Ruby Pendant" hidden Item, the first one processed). Root cause identified: `verifyMigratedItem` compares projected `system` data between the cloned source and the newly created staging-actor copy, but creating the Item on the staging actor via `createEmbeddedDocuments` caused dnd5e 5.3.3 to duplicate an entry in the `system.properties` set — source had `properties: ["gear"]`, the staging copy came back `properties: ["gear","gear"]` — so `deepEqual` failed. This reproduced identically (`attempts: 2`, same error, same phase) after a full Foundry process restart, confirming it is deterministic, not a race. Module runtime (`Runtime mutation and UI hooks`) remained disabled for the rest of the session as a result, per the console warning logged. |
| Verify shared storage remains player-readable | Blocked | Not reached — the migration never advanced past the first hidden Item, so the shared/private split and post-migration read paths were never exercised. |
| Verify private staging data is inaccessible to players | Blocked | Not reached, for the same reason. |
| Verify all expected data against the pre-migration inventory | **FAILED** | Cannot be verified as migrated: the backing actor still holds all 5 original Items, the original 3 hidden-currency entries (2.5 gp fractional included), and the original 3 transaction-log entries, because the migration aborted before touching them (fails safe — no data was lost or corrupted). The private staging actor holds exactly one partially-migrated, verification-failing copy of "Ruby Pendant" and nothing else. |
| Interrupt and resume a disposable migration | Not attempted | Skipped given the base migration path was already blocking; the nice-to-have interruption rehearsal was not pursued. |
| Restart Foundry and recheck persistence and idempotency | Partially done | The full Foundry process was stopped and relaunched once, and the GM logged back in. The migration re-ran automatically, failed identically (attempt counter advanced 1 → 2, same phase and error), and — importantly — did **not** create a duplicate staging Item or lose/duplicate any backing-actor data on retry. So the restart-safety/no-duplication property held even under this failure, but true post-success persistence/idempotency (schema marker advancing once and staying put) could not be exercised because the migration never succeeded. |

## Procedure for the authorized rehearsal

1. Copy the world and its relevant Foundry data to a disposable test location; record the backup path and checksum or timestamp.
2. On v0.1.8, inventory the shared vault, hidden loot, currencies, resources, folders, notes, transaction logs, settings, and shortcut token links. Capture counts and representative values without including private campaign content in public evidence.
3. Install the exact validated v1 candidate only in the disposable environment.
4. Start the world as a GM and retain the console and server logs.
5. Compare post-migration shared and private storage with the pre-migration inventory. Verify that private records are absent from player-readable storage and views.
6. Reopen the world as both GM and player, exercise representative transfers and balances, and restart Foundry once more.
7. If testing interruption, use another disposable copy. Confirm the schema marker remains unset after failure and that a rerun completes without duplicate records.
8. Record tester, date, platform, Foundry version, system/version, candidate commit, package checksum, outcome, and any anomalies below.

## Authorized rehearsal result

### Re-rehearsal (run 2)

| Field | Value |
| --- | --- |
| Tester | background verification agent (re-run 2) |
| Date | 2026-08-10 |
| Platform | Windows 11 Pro (10.0.26200), local machine |
| Foundry VTT | v14 Build 365, isolated data path `test-data/v14-migration`, port 30002 |
| Game system and version | dnd5e 5.3.3 |
| Candidate commit | 7d9b55f9613e76444a600de4d162ccd1bd9893b5 |
| Package checksum | 797531b86f26d3ca72285eb0b4302d77b70d14874e8bd4d6c142d78f5f71dd1a (verified via `sha256sum` before and after extraction) |
| Result | **PASS** |
| Notes or defects | No defects found in the migration logic itself. The automatic first-GM-startup migration completed in a single attempt (`attempts:1`, `phase:"complete"`, `error:null`) against a pre-migration fixture that naturally exercised the exact duplicate-tolerant-array precondition the fix targets (every dnd5e Item's `system.properties` field, a Set-backed field) — no `hidden-item-verification-failed-*` error occurred. All checklist items passed: shared vault stayed player-readable and correct (verified from a real non-GM "Player2" session, not just flags); private staging held all 3 previously-hidden Items with notes/folder intact; the leaked `lootPrepNote`/`lootPrepFolder` on the visible "Masterwork Longsword" was scrubbed from the shared copy and preserved in staging's `migratedItemMetadata` for audit; hidden currency including the 2.5 gp fractional entry migrated intact; `migrationState.fractionalCurrencyWarnings` was present but correctly empty (dnd5e's currency adapter tolerates fractional balances by design — the warning path is for whole-unit systems); transaction logs were split correctly (canonical copy on staging, redacted player-facing projection on shared); `storageSchemaVersion:1` set on both actors; idempotent across both a client reload and a full Foundry process restart, with no duplication in either case. One incidental defect was found and fixed in the *test setup*, not the candidate: a module copy left over from a prior interrupted attempt at `test-data/v14-migration/Data/modules/quartermaster` was missing 13 `test-step*.js` files that v0.1.8's `scripts/module.js` imports, so the whole ESM module silently failed to load while Foundry still reported it "active" — rebuilding it fresh via `git archive` from the protected tag checkout fixed this; it has no bearing on the v1.0.0 candidate, whose own `module.json`/`module.js` do not reference any `test-step*.js` files. One observation, not a defect and not new to this fix: Foundry's client-authoritative sync model still delivers full document data for `visible:false`/unowned actors to every connected client's JS runtime even though the UI (sidebar directory, sheet permission) correctly hides and blocks them — a player using devtools could read raw staging-actor data via `game.actors.get(id)`. This is a platform-wide Foundry characteristic, true before this migration and unaddressed by it, and out of scope here. The optional bonus interruption rehearsal (second disposable world, process killed via a filesystem-based race the instant migration-related writes were detected) was attempted but inconclusive: the migration is so fast (~300ms end to end) that it had already completed and advanced the schema marker before the OS-level process kill actually took effect, so no genuine mid-migration state was captured; the rerun afterward was clean with no duplicates, which is consistent with correct interruption-safety but does not prove it. Recommend: if interruption-recovery coverage is required for the release gate, add a test-only instrumentation hook (feature-flagged, not shipped) that can pause between migration phases, since black-box process-kill timing cannot reliably land inside a sub-second in-process operation. |

### Original rehearsal (run 1, kept for the record)

| Field | Value |
| --- | --- |
| Tester | background verification agent |
| Date | 2026-08-10 |
| Platform | Windows 11 Pro (10.0.26200), local machine |
| Foundry VTT | v14 Build 365, isolated data path `test-data/v14-migration`, port 30002 |
| Game system and version | dnd5e 5.3.3 |
| Candidate commit | 6237480bca9ba9d69116216d30da731dbce604ec |
| Package checksum | 5e7f0b7eca6dfe9e63a272c669d02c964615107a6f12f6d3c40e8ffec384c76e (verified via `sha256sum` before and after use) |
| Result | **FAIL** |
| Notes or defects | Blocking defect: `migrateStorageSchema`'s per-item `verifyMigratedItem` check fails deterministically on hidden dnd5e Items whose `system.properties` set is non-empty (e.g. `["gear"]`). Recreating the Item on the staging actor via `createEmbeddedDocuments(..., {keepId:true})` causes dnd5e 5.3.3 to duplicate an existing entry in that `SetField` (`["gear"] → ["gear","gear"]`), so the exact-shape `deepEqual` comparison against the cloned source data fails and the migration throws `hidden-item-verification-failed-<itemId>` in the `items` phase. Confirmed deterministic across a full process restart (attempt 1 and attempt 2 failed identically on the same item). No data loss occurred — the shared backing actor retained all original Items, hidden-currency entries (including the 2.5 gp fractional entry), and transaction-log entries; the private staging actor was left with exactly one non-duplicated, still-failing partial copy, so retries do not accumulate duplicates. Because the migration never completes, none of the following could be exercised in this rehearsal: shared-vault player-readability post-migration, private-staging inaccessibility to players, loot-prep-note/folder scrubbing from the shared visible Item ("Masterwork Longsword"), the fractional-currency-warning surfacing (`migrationState.fractionalCurrencyWarnings`), transaction-log projection, or the schema-version-advance/idempotency-after-success checks. Recommend the fix normalize/deduplicate Set-type system fields (or compare via a system-aware/tolerant diff) before the exact-equality verification, then re-run this full rehearsal end to end. |

This section has now been completed with a PASS result on re-rehearsal; final release approval remains a human decision.
